{
  "title": "Jacksonville Realtor Finder",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "phase": {
      "title": "Phase",
      "type": "string",
      "enum": [
        "discover",
        "enrich"
      ],
      "editor": "select",
      "default": "discover",
      "description": "Choose which phase to run: discover names or enrich with contact info."
    },
    "city": {
      "title": "City",
      "type": "string",
      "default": "Jacksonville, FL",
      "description": "Target market/city for Realtor search and enrichment."
    },
    "limitPerSource": {
      "title": "Max agents per source",
      "type": "integer",
      "default": 250,
      "description": "Soft cap on number of agents to fetch per directory."
    },
    "proxyGroups": {
      "title": "Apify proxy groups (optional)",
      "type": "array",
      "editor": "stringList",
      "description": "Leave empty to run without proxy."
    },
    "brevoExport": {
      "title": "Also export Brevo-formatted CSV (Phase 2)",
      "type": "boolean",
      "default": true,
      "description": "If true, creates PHASE2_BREVO.csv with EMAIL, SMS, FIRSTNAME, LASTNAME."
    },
    "brevoFileName": {
      "title": "Brevo CSV file name (optional)",
      "type": "string",
      "default": "PHASE2_BREVO.csv",
      "description": "Override default Brevo CSV file name."
    }
  },
  "required": [
    "phase"
  ]
}